# LiveServer VS Code Sample

A simple live server hosting extension for static HTML/CSS Projects.

## Running the example
- Open this example in VS Code 1.47+
- `npm install`
- `npm run watch` or `npm run compile`
- `F5` to start debugging
